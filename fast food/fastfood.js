var form = document.forms["orderSubmission"];
var submit = document.getElementById("submit");
var content = document.getElementById("orderContent");
submit.addEventListener("click", submitOrder());

function submitOrder(content){
    console.log(); 
}